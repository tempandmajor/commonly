/**
 * Comments Section - Simplified to fix TypeScript errors
 * Falls back to SimpleCommentsSection for now
 */
'use client';

// Re-export the simplified version for now
export { default } from './SimpleCommentsSection';
