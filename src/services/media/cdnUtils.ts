export const generateCdnUrl = (url: string, width?: number, height?: number): string => {
  // Currently returns original URL - CDN integration can be added here in the future
  // TODO: Add CDN image optimization when CDN service is configured
  return url;
};
